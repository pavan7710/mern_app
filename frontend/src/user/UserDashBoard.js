import React from 'react'
import Base from "../core/Base"

const UserDashBoard = () => {
    return (
       <Base title="WElcome to User  area" description="This is one is User page" className="container bg-success p-4">
           This is one is User area
       </Base>
    )
}

export default UserDashBoard
